# Tests

Application tests begin in Phase 1. The initial suite will cover the vertical
slice's domain behavior, SQLite persistence, catalog boundaries, and the
create-campaign-to-persistent-Trident acceptance workflow.

This directory is intentionally free of placeholder tests during Phase 0.
