# Tests

The Phase 1 suite covers domain validation, the catalog boundary, custom items,
SQLite persistence, and the create-campaign-to-persistent-Trident acceptance
workflow.

Run it from the repository root with:

```shell
uv run pytest
```
